using UnityEngine;
using System.Collections;


/// <summary>
/// TODO(OMAR) Create a Failsafe ---> a scene that holds only one object always running during the game.
/// The failsafe will make sure to avoid any errors (if an object is not present or something) and Log that.
/// TODO (OMAR) Maybe use same scene for menus (i.e shift camera position) that would cut screen number down (loading, game, menu, +pause(additive) )
/// </summary>
public class ScreenManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
